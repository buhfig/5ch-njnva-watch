# 5ch-njnva-watch
## GitHub Actions schedule note
- `schedule` trigger runs only on the repository default branch workflow file.
- Cron in this repo is UTC. `54 15 * * *` means JST 00:54 (next local day).
